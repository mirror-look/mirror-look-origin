import styled from 'styled-components';

const Story = styled('div')`
  display: flex;
  flex-direction: column;
  font-family: Rubik;
  font-style: normal;
  font-weight: 500;
  font-size: 25px;
  line-height: 40px;
  flex-grow: 1;
  height: 100%;

  margin: 0 15px;

  color: #000000;
`;

export default Story;
