import styled from 'styled-components';

const StyledAdvice = styled.div`
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 45px;
  border: 1px solid white;
  border-radius: 30px;
  background: #ffffff;
  height: 100%;
  filter: drop-shadow(0px 4px 4px rgba(0, 0, 0, 0.25));
`;

export default StyledAdvice;
